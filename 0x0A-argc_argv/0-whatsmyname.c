#include <stdio.h>
/**
 * main - program tat rpints the name followed by a new line
 * @argc : number of commands
 * @argv : array that has coomand line arguments
 *Return: doesnt return .
 */
int main(int argc, char **argv)
{
	(void)argc;
	printf("%s\n", argv[0]);
	return (0);
}
