#include "main.h"

/**
 * mul - function that multiplies 2 integers
 *
 * @a : integer that holds first number
 * @b : integer that holds second number
 *
 * Return: Always 0.
 */
int mul(int a, int b)
{
	int mul;

	mul = a * b;
	return (mul);
}
